name: Real-Time Model Poller

on:
  schedule:
    # GFS 00z (Uploads ~04:30 UTC)
    - cron: '*/15 4,5 * * *'
    # ECMWF 00z (Uploads ~07:10 UTC)
    - cron: '*/15 6,7 * * *'
    # GFS 06z (Uploads ~10:30 UTC)
    - cron: '*/15 10,11 * * *'
    # GFS 12z (Uploads ~16:30 UTC)
    - cron: '*/15 16,17 * * *'
    # ECMWF 12z (Uploads ~19:10 UTC)
    - cron: '*/15 18,19 * * *'
    # GFS 18z (Uploads ~22:30 UTC)
    - cron: '*/15 22,23 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  poll:
    runs-on: ubuntu-latest
    
    # Prevent overlapping poller runs
    concurrency:
      group: weather-poller
      cancel-in-progress: false

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install cfgrib xarray pandas eccodes requests numpy ecmwf-opendata matplotlib openpyxl kaggle cartopy imageio

      - name: Create required directories
        run: |
          mkdir -p data/ecmwf
          mkdir -p data/gfs
          mkdir -p data/normals
          mkdir -p data/open_meteo
          mkdir -p data/weights
          mkdir -p outputs/maps

      - name: Copy normals file
        run: |
          cp data/us_daily_normals.csv data/normals/us_daily_normals.csv || true

      - name: Run Model Poller
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          KAGGLE_USERNAME: ${{ secrets.KAGGLE_USERNAME }}
          KAGGLE_KEY: ${{ secrets.KAGGLE_KEY }}
          EIA_KEY: ${{ secrets.EIA_KEY }}
        run: |
          python scripts/poll_models.py
          python scripts/generate_maps.py || echo "[WARN] Map generation failed."

      - name: Trigger Kaggle AI Inference Kernel
        env:
          KAGGLE_USERNAME: ${{ secrets.KAGGLE_USERNAME }}
          KAGGLE_KEY: ${{ secrets.KAGGLE_KEY }}
        run: |
          pip install kaggle -q
          kaggle kernels push -p scripts/kaggle_env || echo "[INFO] Kernel already up-to-date"
          echo "Kernel triggered. Waiting for completion..."

      - name: Poll Kaggle until kernel completes
        env:
          KAGGLE_USERNAME: ${{ secrets.KAGGLE_USERNAME }}
          KAGGLE_KEY: ${{ secrets.KAGGLE_KEY }}
        run: |
          for i in $(seq 1 60); do
            STATUS=$(kaggle kernels status prateekriders/weather-dd-ai-inference 2>&1)
            echo "[$i/60] $STATUS"
            echo "$STATUS" | grep -q "COMPLETE" && echo "Kernel complete!" && exit 0
            echo "$STATUS" | grep -q "ERROR" && echo "Kernel errored!" && exit 1
            sleep 60
          done
          echo "[WARN] Timed out waiting for kernel (60 min max)"

      - name: Download Kaggle kernel output
        env:
          KAGGLE_USERNAME: ${{ secrets.KAGGLE_USERNAME }}
          KAGGLE_KEY: ${{ secrets.KAGGLE_KEY }}
        run: |
          mkdir -p data/ai_models
          kaggle kernels output prateekriders/weather-dd-ai-inference -p /tmp/kaggle_out
          # Copy any CSV files to data/ai_models/
          find /tmp/kaggle_out -name "*.csv" -exec cp {} data/ai_models/ \;
          echo "Downloaded files:"
          ls -la /tmp/kaggle_out/ || true
          ls -la data/ai_models/ || true

      - name: Commit pipeline state and AI model results
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add data/pipeline_state.json || true
          git add outputs/*.csv outputs/*.png outputs/*.xlsx outputs/historical_monthly_charts/*.png outputs/maps/*.gif || true
          git add data/ecmwf/*_tdd.csv data/gfs/*_tdd.csv || true
          git add data/ai_models/ || true
          git add data/weights/ data/normals/ || true
          git diff --staged --quiet || (git commit -m "Auto-update: Pipeline state & metrics $(date '+%Y-%m-%dT%H:%M:%SZ')" && git push)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
